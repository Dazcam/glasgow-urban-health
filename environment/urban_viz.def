Bootstrap: docker
From: python:3.9-slim

%labels
    Maintainer YourName
    Version 1.0
    Description "Glasgow Urban Analytics Dashboard - Research Software Project"

%post
    # Install system-level geospatial dependencies (essential for GeoPandas)
    apt-get update && apt-get install -y \
        build-essential \
        libgdal-dev \
        gdal-bin \
        libproj-dev \
        python3-dev

    # Clean up to keep the image lightweight
    apt-get clean
    rm -rf /var/lib/apt/lists/*

    # Install Python packages
    pip install --no-cache-dir \
        pandas \
        geopandas \
        streamlit \
        folium \
        streamlit-folium

%environment
    # Set the port for Streamlit
    export STREAMLIT_SERVER_PORT=8501
    export LC_ALL=C.UTF-8
    export LANG=C.UTF-8

%runscript
    # This runs when you call 'singularity run'
    exec streamlit run "$@"

%help
    This container runs the Glasgow Urban Analytics Dashboard.
    Usage:
    singularity run urban_viz.sif app.py
